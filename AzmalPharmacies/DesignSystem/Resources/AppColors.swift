import SwiftUI

enum AppColors {
    static let primary = Color(.primaryApp)
    static let primaryDark = Color(.appDark)
    static let disabledButton = Color(.disabledButton)
    static let white = Color(.appWhite)
    static let background = Color(.systemGroupedBackground)
    static let surface = Color(.systemBackground)
    static let textPrimary = Color(.primaryApp)
    static let textSecondary = Color(.appSecondary)
    static let border = Color(.separator)
    static let success = Color(red: 0.10, green: 0.55, blue: 0.30)
}
